<div>

  <div class="row mb-4 project postborder">
    <div class="col-md-8">
      <h2>
        <a class="colordark" href="/project/{{id}}">{{name}}</a>
      </h2>
    </div>
    <div class="col-md-4">
      <h2>
        <p class="alignr">Created by {{user.name}} on {{format_date date_created}}</p>
      </h2>
    </div>
    <div class="col-md-12 whitebackg minh">
      <p>
        {{description}}
      </p>
    </div>
  </div>

  {{!-- <div class="col-md-4">
      <p class="colorslate">dataid: {{id}}</p>
        <button class="btn btn-sm btn-danger" data-id="{{id}}">DELETE</button>
  </div> --}}

  {{! SO i need an if user = post creator then delete and update --}}
  {{!-- {{#if (ifeq user.name profile.user.name) }}
  <p class="colorslate">We are the same</p>
  {{else}}
  <p class="colorslate">We are different</p>
  <p class="colorslate">username: {{user.name}}</p>
  <p class="colorslate">name: {{user.user.email}}</p>
  <p class="colorslate">project user id: {{user_id}}</p>
  {{/if}} --}}


  {{! Start of comment entering--}}
  {{#if logged_in}}
  <div class="row">      
  <div class="col-md-12 colorslate">
    <h3 class="margin10">Would you like to add a comment?</h3>

      <div class="form-group">
        <label for="comment"></label> {{! was for="project-desc" --}}
        <textarea class="form-input col-md-12 comment" id="comment" name="comment"></textarea> {{! was id="project-desc" name="project-desc" --}}
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary margin10">Add your Comment</button>
      </div>
    </form>
  </div>
  </div>
      
  {{else}}
  <a class="btn btn-primary" href="/login">Login to Leave a Comment</a>
  {{/if}}

</div>

<script src="./js/project.js"></script>