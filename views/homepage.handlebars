<!-- Loop over each object in the projects array -->
{{#each projects as |project| }}

{{!-- <div class="row mb-4 project">
  
  <div class="col-md-5">
    <h2>
      <a href="/project/{{project.id}}">{{project.name}}</a>
          <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed!
    </p>
    <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
  </div>
  
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div> --}}

  <div class="row mb-4 project postborder">
    <div class="col-md-8">
      <h2>
        <a class="colordark" href="/project/{{project.id}}">{{project.name}}</a>
      </h2>
    </div>
    <div class="col-md-4">
      <h2>
        <p class="alignr">Created by {{project.user.name}} on {{format_date project.date_created}}</p>
      </h2>
    </div>
    <div class="col-md-12 whitebackg minh">
      <p>
        {{project.description}}
      </p>
    </div>
  </div>
{{/each}}
